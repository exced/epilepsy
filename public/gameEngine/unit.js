/**
 * @param {String} name
 * @param {Number} row
 * @param {Number} column
 * @param {Boolean} superposable
 */
function Unit(name, row, column, superposable) {
    this.name = name;
    this.row = row;
    this.column = column;
    this.superposable = superposable;
}
